<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="bpmn_designer.SequenceFlowPanel" owl="1">
        <div t-if="isConditionalFlow">
            <h6 class="mt-3">分支条件</h6>
            <p class="text-muted small">当流程到达上一个分支节点时，将根据此条件判断是否执行该路径。</p>
            <hr class="my-2"/>
            <BpmnDomainField
                resModel="props.targetModel"
                domain="businessObject.get('odoo:conditionDomain') || '[]'"
                update="onDomainUpdate"
            />
        </div>
        <div t-else="">
            <p class="text-muted fst-italic mt-3">
                <i class="fa fa-info-circle me-1"/>
                此连线不源于一个分支节点，因此无法配置执行条件。
            </p>
        </div>
    </t>
</templates>